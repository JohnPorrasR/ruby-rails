<div class="image-box">
    <div class="image-content">
      <%= image_tag image.photo.thumb.url %>
      <h2><%= image.name %></h2>
      <p><%= image.category %> - <%= image.description %></p>
      <strom><%= image.tags %></strom>
    </div>
</div>